Научился пользоваться разграничениями прав в линукс оболочке с помощью таких команд как: chomd, chown, groupadd, useradd 

Крaткое описание скриншотов:

**1 часть Подготовка среды**
    создал папку под 3ью практическую и данный файл. 
    Так же установил ACL(нужен для работы с расширенными права доступа.
 
 
**2 часть Создание групп и юзеров**
    Создал группы devops, audit. Создал юзеров alice, bob, carol.
    Установил пароли для каждого пользователя
    devops: alice, bob
    audit: carol
    
**3 часть chmod and chown 1.1-1.2**
    Наглядно показывал пользование командой chmod и chown.
    При 750 bob не мог создавать файла, а после присвоения 770 - смог.
    Carol не имеет доступа к '/srv/project/code' (так как в конце 0 - 750 или 750)
    На файле '/srv/project/code/reoprt/rp_1.md' была смена владельца группы на audit и его смогла читать carol(она в группе audit)


**4 часть Настройка ACL**
    ACL позволил дать доступ на чтение '/srv/project/code' без изменения группы.
    В выводе команды 'ls -la' появился у конце +, что показывает использование ACL.


**5 часть Настройка SUDO  1.1-1.2**
    Распределил sudo привелегий между пользователями.
    Alice - полный sudo
    Bob - только пакетный менеджер
    Carol - только просмотр логов.


**6 часть Изучение PAM**
    Изучил что такое PAM.
    pam.unix.so - основной модуль для проверки паролей с помощью хэшей '/etc/shadow'
    Для установки минимальной длины пароля нужно добавить параметр 'minlen=12' в строчку с 'pam_pwquality'
